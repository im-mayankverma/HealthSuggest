Django==4.2.11

# Gunicorn is used for production servers on Linux (optional for local dev on Windows)
gunicorn==21.2.0

# Database driver (choose ONE: sqlite3 is built‑in, so these are optional)
# For MySQL:
mysqlclient==2.2.4
# Or if you use PostgreSQL instead of MySQL:
# psycopg2-binary==2.9.9

# For handling environment variables (if you want)
python-dotenv==1.0.1
